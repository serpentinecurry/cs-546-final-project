<style>
  .error-wrapper {
    max-width: 600px;
    margin: 5rem auto;
    padding: 3rem;
    background-color: #fff3f3;
    border: 1px solid #f8d7da;
    border-radius: 16px;
    text-align: center;
    box-shadow: 0 8px 30px rgba(0, 0, 0, 0.05);
    animation: fadeIn 0.5s ease-in-out;
  }

  .error-wrapper h2 {
    color: #dc3545;
    font-size: 2rem;
    margin-bottom: 1rem;
  }

  .error-wrapper p {
    color: #842029;
    font-size: 1.2rem;
    margin-bottom: 2rem;
  }

  .error-wrapper a {
    text-decoration: none;
    color: #fff;
    background-color: #0d6efd;
    padding: 0.6rem 1.5rem;
    border-radius: 8px;
    transition: all 0.3s ease;
    font-weight: 500;
  }

  .error-wrapper a:hover {
    background-color: #084298;
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(12px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>

<div class="error-wrapper">
  <h2><i class="bi bi-exclamation-circle-fill me-2"></i>Something Went Wrong</h2>
  <p id="error">{{error}}</p>
  <a href="/"><i class="bi bi-house-door me-1"></i>Return to Home</a>
</div>
